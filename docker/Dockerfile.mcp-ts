# C4A TypeScript MCP Server Dockerfile
# 用于 mcp-dsl 和 mcp-code

FROM oven/bun:1.1-alpine

WORKDIR /app

# Install curl for health checks
RUN apk add --no-cache curl

# Copy workspace config
COPY package.json bun.lockb* ./
COPY tsconfig.json ./

# Copy package.json files for dependency resolution
COPY packages/core/package.json packages/core/
COPY packages/mcp-dsl/package.json packages/mcp-dsl/
COPY packages/mcp-code/package.json packages/mcp-code/

# Install dependencies
RUN bun install --frozen-lockfile || bun install

# Copy source files
COPY packages/core packages/core
COPY packages/mcp-dsl packages/mcp-dsl
COPY packages/mcp-code packages/mcp-code

# Default to HTTP transport for Docker
ENV MCP_TRANSPORT=streamable-http

# MCP_SERVER and MCP_PORT are set via docker-compose
# MCP_SERVER: mcp-dsl or mcp-code
# MCP_PORT: 8051 (dsl) or 8052 (code)

# Run the specified MCP server
CMD ["sh", "-c", "bun run packages/${MCP_SERVER}/src/index.ts"]
