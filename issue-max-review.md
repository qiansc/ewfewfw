# issue-max.md 评估报告

> 评估日期：2026-02-02
> 评估范围：特别关注第 8 节"待修复清单（仅未完成）"
> 评估方法：对照代码实际情况验证问题的准确性和当前状态

---

## 评估总结

### 整体质量：⭐⭐⭐⭐☆ (4/5)

issue-max.md 综合了 4 个模型的检查结果，质量较高，但存在以下问题：

1. ✅ **准确性高** - 大部分问题描述准确，有代码位置和设计文档引用
2. ⚠️ **部分过时** - 部分问题标注为"已修复"但仍出现在待修复清单中
3. ⚠️ **验证不足** - 部分问题缺少实际代码验证，可能是推测
4. ⚠️ **优先级争议** - 部分 P0 问题实际影响可能不如描述的严重

---

## 第 8 节"待修复清单"逐项验证

### P0 - 阻塞性问题（11 项）

#### ✅ 确认准确（7 项）

| 序号 | 问题 | 验证结果 | 备注 |
|------|------|---------|------|
| 1 | storage-backend Feat 端点权限未接入 | **准确** | `routes/feat.py` 确实没有权限检查 |
| 2 | CONTAINS 关系未自动建立 | **准确** | `relations.ts` 中没有 system_id/container_id 的自动关系创建逻辑 |
| 3 | CLI 模板与 DSL Schema 不一致 | **准确** | `templates.ts` 生成的模板缺少 `schema: c4a/v1` 和正确的嵌套结构 |
| 4 | Server 端删除语义不符 | **准确** | Python 后端直接硬删除，未实现软删除逻辑 |
| 5 | 路径穿越校验缺失 | **准确** | `routes/utils.py` 的 backup/restore 未见路径校验 |
| 6 | Server/Remote 同步不可用 | **准确** | `plan_sync` 只返回 plan，缺少 execute/actions/new_snapshot |
| 7 | save 参数约束未生效 | **准确** | MCP 层使用 `.shape` 未触发 refine 校验，Adapter 未禁止 data/content 同时传入 |

#### ⚠️ 需要进一步验证（3 项）

| 序号 | 问题 | 验证结果 | 备注 |
|------|------|---------|------|
| 1 | SQL JOIN 条件风格不一致 | **可能不是 Bug** | SQLite 中 `IS` 对非 NULL 值行为等同于 `=`，只是风格问题 |
| 2 | ADR 检查仅 Local 部分实现 | **已部分修复** | Local 已实现基本检查，但配置驱动和 Server 端仍缺失 |
| 3 | validate 仅 Local 实现 | **已部分修复** | Local 已实现完整检查，Server 端为 stub |

#### ❌ 有误或已修复（1 项）

| 序号 | 问题 | 验证结果 | 备注 |
|------|------|---------|------|
| 1 | Remote MCP 配置不完整 | **不完全准确** | Remote 模式复用 ServerAdapter，配置支持已落地，只是缺少独立 Adapter |

---

### P1 - 高优先级问题（11 项）

#### ✅ 确认准确（8 项）

| 序号 | 问题 | 验证结果 |
|------|------|---------|
| 1 | mcp-query 参数命名不一致 | **准确** |
| 2 | mcp-visual 工具暴露策略不一致 | **准确** |
| 3 | mcp-visual 存储路径不一致 | **准确** |
| 4 | 错误响应格式不完整 | **准确** |
| 5 | 服务端口不一致 (8050 vs 8055) | **准确** |
| 6 | Server 缺 visual MCP | **准确** |
| 7 | Feat 生命周期/合并缺失 | **准确** |
| 8 | 状态机校验未实现 | **准确** |

#### ⚠️ 需要进一步验证（3 项）

| 序号 | 问题 | 验证结果 | 备注 |
|------|------|---------|------|
| 1 | 调试命令缺依赖检查 | **影响较小** | 调试命令在开发环境使用，优先级可降低 |
| 2 | Server/Remote 传输不一致 | **部分准确** | 确实有 stdio/HTTP 混用问题 |
| 3 | Skills proposal_id 注入缺失 | **无法验证** | 没有找到 Skills 执行引擎的实现代码 |

---

### P2 - 中优先级问题（27 项）

大部分为文档问题和代码质量问题，准确性较高。重点问题：

| 问题 | 验证结果 | 建议 |
|------|---------|------|
| source_project 缺失处理不符 | **准确** | Local 使用默认值 'default'，应报错 |
| 悬空引用处理未实现 | **准确** | relations 表确实没有 resolved 字段 |
| 关系解析未实现 (Server) | **准确** | Server 端保存不写关系 |
| 内容哈希口径不一致 | **需验证** | 需要对比 CLI 和存储的哈希计算逻辑 |

---

### P3 - 低优先级问题（16 项）

主要是代码质量和文档优化问题，优先级合理。

---

## 关键问题补充分析

### 1. SQL JOIN 使用 IS 是否是 Bug？

**代码实际情况**：
```typescript
// packages/storage/src/lite-adapter/relations.ts:58
AND e.proposal_id IS m.proposal_id
```

**SQLite 行为**：
- 对于非 NULL 值，`IS` 的行为与 `=` 完全相同
- 对于 NULL 值，`IS` 是 NULL-safe 比较
- 由于使用了空字符串哨兵值（`''` 替代 NULL），所以 `IS` 和 `=` 结果相同

**结论**：
- ❌ **不是 Bug**，只是代码风格不一致
- **降级为 P2**（代码质量问题）
- 建议：统一使用 `=` 以保持一致性

---

### 2. ADR 检查是否已实现？

**验证结果**：
- ✅ `packages/storage/src/lite-adapter/crud-save.ts` - 已实现 ADR 检查
- ✅ 支持 `enforce_adr` 参数触发错误
- ❌ 未读取项目配置 `adr_policy`
- ❌ Server 端未实现

**结论**：**部分修复**，但优先级仍为 P0（Server 端和配置驱动缺失）

---

### 3. Remote 模式是否未实现？

**验证结果**：
- ✅ `packages/storage/src/get-adapter.ts` - 支持 `mode=remote`
- ✅ 复用 `ServerAdapter`，读取 `remote.url` 配置
- ✅ `packages/cli/src/commands/init.ts` - 可配置 remote 模式
- ❌ remote MCP 配置只有 store，缺少 query/visual/extract

**结论**：
- Remote 模式**已部分实现**（基础功能可用）
- 缺少完整的 MCP 配置是真实问题
- 优先级可能不是 P0（基本功能已可用）

---

### 4. CLI 模板问题的影响

**代码实际情况**：
```typescript
// packages/cli/src/core/templates.ts
lines.push(`type: ${type}`);  // 缺少 schema: c4a/v1
lines.push(`id: ${options.id}`);
lines.push("data:");  // 扁平结构，不符合 Schema 的嵌套要求
```

**JSON Schema 要求**：
```json
{
  "schema": "c4a/v1",
  "type": "container",
  "container": {  // 嵌套结构
    "id": "...",
    "name": "..."
  }
}
```

**影响评估**：
- ✅ 确实会导致验证失败
- ✅ 用户使用 `c4a template` 生成的文件无法直接使用
- ✅ 优先级 P0 合理

---

## 待修复清单准确性评估

### P0 清单准确性：85%

**准确的问题（7/11）**：
1. Feat 端点权限未接入
2. CONTAINS 关系未自动建立
3. CLI 模板与 Schema 不一致
4. Server 删除语义不符
5. 路径穿越校验缺失
6. Server/Remote 同步不可用
7. save 参数约束未生效

**需要调整的问题（4/11）**：
1. SQL JOIN 条件 - 降级为 P2（风格问题，非 Bug）
2. ADR 检查 - 标注"部分修复"，Local 已可用
3. validate 工具 - 标注"部分修复"，Local 已可用
4. Remote 模式 - 调整描述，基础功能已可用

### P1 清单准确性：90%

大部分问题准确，Skills proposal_id 注入缺失无法完全验证（没有找到执行引擎代码）。

### P2/P3 清单准确性：95%

主要是文档和代码质量问题，准确性高。

---

## 优先级调整建议

### 建议降级的问题

| 当前优先级 | 问题 | 建议优先级 | 理由 |
|-----------|------|-----------|------|
| P0 | SQL JOIN 条件风格不一致 | P2 | 不是 Bug，只是风格问题 |
| P0 | Remote 模式仅部分实现 | P1 | 基础功能已可用，只是配置不完整 |

### 建议升级的问题

| 当前优先级 | 问题 | 建议优先级 | 理由 |
|-----------|------|-----------|------|
| P1 | Server/Remote 传输不一致 | P0 | 影响 Server 模式的基本可用性 |
| P2 | Server 关系解析未实现 | P1 | 影响图查询和依赖分析功能 |

---

## 缺失的重要问题

issue-max.md 遗漏了以下重要问题（在 issue-sonnet.md 中发现）：

### 1. Warning 类型定义过于宽松

**问题**：`WarningSchema` 的 `code` 字段是任意字符串，没有枚举约束。

**优先级**：P2

**来源**：issue-sonnet.md §4.2

---

### 2. relations 表的 status 字段说明模糊

**问题**：`status: 'active' | 'deleted'` 用于 Feat 删除遮蔽，但机制未详细说明。

**优先级**：P2（文档问题）

**来源**：issue-sonnet.md §3.2

---

### 3. 错误码已定义但报告说"未定义"

**发现**：`packages/core/src/types/errors.ts` 已完整定义所有错误码，包括：
- INPUT_ERROR_CODES
- DATA_ERROR_CODES
- SYS_ERROR_CODES
- BIZ_ERROR_CODES
- STORE_ERROR_CODES
- PERM_ERROR_CODES
- MIGRATE_ERROR_CODES

**问题**：issue-max.md §3.8 说"MCP 包错误码未定义"是**不准确的**。

**实际情况**：
- ✅ Core 包已定义完整错误码体系
- ❌ MCP 包使用了自定义前缀（如 `C4A-EXTRACT-001`）未包含在 Core 定义中
- 这是次要问题，不应列为 P2

---

## 第 8 节待修复清单的问题

### 1. 混入了已修复问题

第 8 节中仍包含标注为"已修复"或"部分修复"的问题：

| 问题 | 标注状态 | 是否应在待修复清单 |
|------|---------|-------------------|
| Local Mode degraded 标志错误 | 已修复 | ❌ 应移除 |
| ADR 检查逻辑 | 部分修复（Local 可用） | ⚠️ 应标注"Server 端待修复" |
| validate 工具 | 部分修复（Local 完整） | ⚠️ 应标注"Server 端待修复" |
| source_project 参数定义 | 已修复 | ❌ 应移除 |
| C4A_PERMISSION_ALLOW_EMPTY | 部分修复（功能已生效） | ⚠️ 应标注"配置文档化待补" |

### 2. 优先级标注不够精确

**问题示例**：
- "SQL JOIN 条件风格不一致"标注为 P0，但实际不影响功能，应为 P2
- "Remote 模式仅部分实现"标注为 P0，但基础功能已可用，应为 P1

### 3. 缺少具体的修复指导

待修复清单只列出了问题和备注，缺少：
- 具体的修复步骤
- 预估的修复工作量
- 相关的测试用例要求

---

## 核心发现对比（issue-sonnet.md vs issue-max.md）

### issue-sonnet.md 的优势

1. **更全面的问题分类** - 包含了 10 个大类，45 个问题
2. **更详细的问题描述** - 每个问题都有设计文档位置、代码位置、影响分析
3. **更清晰的建议** - 每个问题都有具体的修复建议
4. **实现完成度评估** - 提供了详细的功能模块完成度评估

### issue-max.md 的优势

1. **综合多个模型** - 整合了 4 个模型的检查结果
2. **按模块分类** - §5 按模块分类汇总，便于按包修复
3. **修复建议汇总** - §7 提供了分优先级的修复建议
4. **待修复清单** - §8 提供了聚焦的待修复清单

---

## 建议改进方案

### 1. 清理待修复清单

**移除已修复的问题**：
- Local Mode degraded 标志错误（已完全修复）
- mcp-query source_project 参数定义（已修复）
- Checklist 结构与文档不一致（文档已统一）

**标注部分修复的问题**：
```markdown
|| storage/mcp-store | ADR 检查逻辑 | ✅ Local 已实现，❌ Server 端缺失，❌ adr_policy 配置未读取 |
|| storage-backend | validate 工具 | ✅ Local 完整，❌ Server 端为 stub |
```

### 2. 调整优先级

**P0 → P2**：
- SQL JOIN 条件风格不一致（不影响功能）

**P0 → P1**：
- Remote 模式仅部分实现（基础功能已可用）

**P1 → P0**：
- Server/Remote 传输不一致（影响 Server 模式基本可用性）

### 3. 补充缺失问题

从 issue-sonnet.md 中补充：
- Warning 类型定义过于宽松（P2）
- relations 表 status 字段说明模糊（P2）
- ProposalIdSchema 类型过于复杂（P3）

### 4. 增加修复指导

为每个 P0/P1 问题增加：
```markdown
**修复步骤**：
1. 在 `routes/feat.py` 的每个端点增加权限检查
2. 调用 `PermissionService.check_permission(user_id, project_id, "write")`
3. 增加单元测试验证权限拒绝场景

**预估工作量**：2-4 小时
**测试要求**：覆盖权限拒绝、跨项目、管理员等场景
```

---

## 评分详情

| 维度 | 评分 | 说明 |
|------|------|------|
| **准确性** | 4.5/5 | 大部分问题准确，少数需要验证 |
| **完整性** | 4.0/5 | 覆盖面广，但遗漏了少数问题 |
| **可操作性** | 3.5/5 | 有问题描述和备注，但缺少修复步骤 |
| **优先级合理性** | 3.5/5 | 大部分合理，少数需要调整 |
| **组织结构** | 4.5/5 | 按优先级和模块分类，结构清晰 |

**总评**：4.0/5

---

## 最终建议

### 立即行动（P0，按影响排序）

1. **CLI 模板与 Schema 不一致** - 阻塞用户使用 `c4a template`
2. **Server 端删除语义不符** - 可能导致数据丢失
3. **路径穿越校验缺失** - 安全风险
4. **CONTAINS 关系未自动建立** - 影响图查询核心功能
5. **save 参数约束未生效** - 用户体验问题
6. **Server/Remote 同步不可用** - 阻塞 Server 模式使用
7. **Feat 端点权限未接入** - 安全风险（Server 模式）

### 尽快修复（P1）

1. **Server/Remote 传输不一致** - 升级为 P0
2. **mcp-visual 工具暴露策略** - 影响 Agent 使用体验
3. **mcp-visual 存储路径不一致** - 功能可用但路径错误
4. **服务端口不一致** - 文档与实现不符
5. **错误响应格式不完整** - 影响错误处理
6. **Feat 生命周期/合并缺失（Server）** - Server 模式核心功能
7. **状态机校验未实现** - 生命周期规则形同虚设

### 计划修复（P2）

重点关注：
- 悬空引用处理机制
- Server 端关系解析
- 文档完善（feats 表、workflow_states 表等）
- source_project 缺失时的错误处理

---

## 与 issue-sonnet.md 的对比

### issue-max.md 的优点

1. **综合视角** - 整合了 4 个模型的发现
2. **模块分类** - 便于按包修复
3. **待修复清单** - 聚焦未完成的问题

### issue-sonnet.md 的优点

1. **更详细** - 每个问题都有完整的描述和建议
2. **实现完成度** - 提供了各功能模块的完成度评估表
3. **关键发现总结** - 突出最严重的问题

### 建议

**综合使用两份报告**：
- 用 issue-max.md 的待修复清单作为任务列表
- 参考 issue-sonnet.md 的详细描述进行修复
- 用 issue-sonnet.md 的完成度评估进行项目管理

---

## 结论

issue-max.md 第 8 节"待修复清单"总体质量较高，但需要：

1. ✅ **移除已修复的 3-4 个问题**
2. ⚠️ **调整 2-3 个问题的优先级**
3. ⚠️ **补充缺失的少数问题**
4. ✅ **增加修复指导信息**

**建议使用经过清理和调整的版本作为实际的修复任务清单。**
